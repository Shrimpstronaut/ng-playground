// I'am studip and didn't think about Account being a reserved name.
// Also I'm too lazy to manually import my Account class every time.
export interface AppAccount {
  id: number;
  email: string;
}
